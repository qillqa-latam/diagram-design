# Semantic Patterns

Seven **behavioral semantic patterns** encode recurring system-design narratives as factory functions. Each produces a fully configured diagram instance.

| Pattern | Description |
|---------|-------------|
| Fan-in Queue / Bottleneck | Multiple sources feeding a queue with capacity constraints |
| Stage Framework | Semantic slots across pipeline stages |
| Unstructured Input → Structured Artifact | Raw input transformed into governed output |
| Paired Policy-Evaluation Traces | Side-by-side policy evaluation flows |
| Secure Paved Road | Golden path with guardrails |
| Governance / Control Catalog | Control framework catalog layout |
| Compensating Security Layers | Defense-in-depth layer model |

Select a pattern from the sidebar to see a live preview and usage notes.
